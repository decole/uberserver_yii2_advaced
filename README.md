#Uberserver.ru on Yii2
---------------------
как поставить:
 - `docker-compose up -d`
 
 Запуск первый раз:
 - `docker-compose run backend bash`
 - `composer install`
 - `php init` -> 0 = Development / 1 = Production
 - `yii migrate` yes
